CREATE TABLE memos
(
    id int
    AUTO_INCREMENT,
    memo varchar
    (100),
    PRIMARY KEY
    (id)
);

    INSERT INTO memos
        (memo)
    values
        ('ichigo.chocomint'),
        ('banana.chocomint'),
        ('pinapple.chocomint')
;
